{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [  
        {
            "label": "CompileCapnpSchema",
            "detail": "Compiles the Cap'n Proto Schema file to a binary format",
            "type": "shell",
            "command": "capnp compile -o- ${workspaceFolder}/Fundament.Capstone.Compiler/Schema/schema.capnp > schema.bin",
        }
    ]
}